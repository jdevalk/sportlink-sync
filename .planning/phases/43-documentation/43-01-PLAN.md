---
phase: 43-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/database-schema.md
  - docs/sync-architecture.md
  - docs/pipeline-people.md
  - docs/pipeline-nikki.md
  - docs/pipeline-teams.md
  - docs/pipeline-functions.md
  - docs/pipeline-freescout.md
  - docs/pipeline-discipline.md
  - docs/reverse-sync.md
  - docs/installation.md
  - docs/operations.md
  - docs/troubleshooting.md
  - docs/utility-scripts.md
  - CLAUDE.md
  - package.json
autonomous: true

must_haves:
  truths:
    - "Zero stadion references remain in docs/ directory (excluding historical migration context)"
    - "CLAUDE.md uses rondo_club_id instead of stadion_id"
    - "package.json script name uses rondo-club instead of stadion"
    - "All SQL examples in docs reference rondo_club_* tables and rondo_club_id columns"
    - "All code examples in docs reference rondoClub variable names"
  artifacts:
    - path: "docs/database-schema.md"
      provides: "Updated schema docs with rondo_club naming"
      contains: "rondo_club_members"
    - path: "docs/sync-architecture.md"
      provides: "Updated architecture docs"
      contains: "rondo_club_id"
    - path: "CLAUDE.md"
      provides: "Updated AI context with rondo_club naming"
      contains: "rondo_club_id"
    - path: "package.json"
      provides: "Updated script names"
      contains: "prepare-rondo-club"
  key_links:
    - from: "docs/database-schema.md"
      to: "lib/rondo-club-db.js"
      via: "Table and column names must match actual schema"
      pattern: "rondo_club_(members|parents|teams|commissies)"
---

<objective>
Update all stadion references to rondo_club in the rondo-sync repository's documentation files, CLAUDE.md, and package.json.

Purpose: Complete requirement DOCS-01 (docs/ updated), DOCS-02 (CLAUDE.md updated), and DOCS-03 (package.json updated) of the v3.2 Stadion-to-Rondo Rename milestone.
Output: 15 files with zero remaining stadion references (except in intentional historical/migration context).
</objective>

<execution_context>
@/Users/joostdevalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostdevalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-code-references/42-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename stadion references in all 13 docs/ files</name>
  <files>
    docs/database-schema.md
    docs/sync-architecture.md
    docs/pipeline-people.md
    docs/pipeline-nikki.md
    docs/pipeline-teams.md
    docs/pipeline-functions.md
    docs/pipeline-freescout.md
    docs/pipeline-discipline.md
    docs/reverse-sync.md
    docs/installation.md
    docs/operations.md
    docs/troubleshooting.md
    docs/utility-scripts.md
  </files>
  <action>
    Rename all stadion references to rondo_club across all 13 docs/ files. There are approximately 200 total occurrences across these files.

    Apply these renaming rules consistently (same rules used in Phase 42 code rename):

    **Table names (SQL context):**
    - `stadion_members` -> `rondo_club_members`
    - `stadion_parents` -> `rondo_club_parents`
    - `stadion_teams` -> `rondo_club_teams`
    - `stadion_commissies` -> `rondo_club_commissies`
    - `stadion_change_detections` -> `rondo_club_change_detections`
    - `stadion_reverse_sync_log` -> `rondo_club_reverse_sync_log`

    **Column names (SQL context):**
    - `stadion_id` -> `rondo_club_id`
    - `member_stadion_modified` -> `member_rondo_club_modified`
    - `parent_stadion_modified` -> `parent_rondo_club_modified`
    - `team_stadion_modified` -> `team_rondo_club_modified`
    - `commissie_stadion_modified` -> `commissie_rondo_club_modified`

    **Variable names (JS code examples):**
    - `stadionId` -> `rondoClubId`
    - `stadionData` -> `rondoClubData`
    - `stadionMember` -> `rondoClubMember`
    - Any other `stadion` camelCase -> `rondoClub` camelCase

    **Function names:**
    - `getAllStadionPeople` -> `getAllRondoClubPeople`
    - `getStadionMember` -> `getRondoClubMember`
    - `fetchAllPeopleFromStadion` -> `fetchAllPeopleFromRondoClub`
    - Any other `Stadion` PascalCase -> `RondoClub` PascalCase

    **File references (when mentioned in docs):**
    - `stadion-sync.sqlite` -> reference as historical/stale only (it already is)

    **Prose/comments:**
    - "stadion ID" -> "Rondo Club ID"
    - "Stadion WordPress" -> "Rondo Club WordPress" (if any remain)
    - "stadion_id mapping" -> "rondo_club_id mapping"
    - Section headings referencing stadion -> rondo_club

    **Special attention for database-schema.md:** This file has the most references (39). Carefully update all table definitions, column listings, SQL examples, and relationship descriptions.

    **Special attention for sync-architecture.md:** The table of contents has an anchor `#reverse-sync-stadion-to-sportlink` that should become `#reverse-sync-rondo-club-to-sportlink`. Update both the anchor in the TOC link and the section heading it points to.

    **Do NOT change:**
    - References to "Sportlink" (that is a separate external system)
    - Any migration-specific context that explains the rename itself
    - The actual Mermaid diagrams that use `ST` as a node label (that stands for Rondo Club, not Stadion)

    Process each file systematically. For large files (database-schema.md, reverse-sync.md, sync-architecture.md), work through section by section.
  </action>
  <verify>
    Run: `grep -ri 'stadion' docs/` — should return zero results.

    Spot-check key files:
    - `grep 'rondo_club_members' docs/database-schema.md` returns multiple matches
    - `grep 'rondo_club_id' docs/pipeline-people.md` returns matches
    - `grep 'rondo_club_change_detections' docs/reverse-sync.md` returns matches
  </verify>
  <done>
    All 13 docs/ files contain zero stadion references. All table names, column names, variable names, function names, and prose in docs match the rondo_club naming used in the actual codebase after Phase 42.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and package.json</name>
  <files>
    CLAUDE.md
    package.json
  </files>
  <action>
    **CLAUDE.md** — Line 27 contains:
    ```
    Running locally causes duplicate entries because each machine has its own SQLite database with different `stadion_id` mappings.
    ```
    Change `stadion_id` to `rondo_club_id`.

    **package.json** — Line 10 contains:
    ```
    "prepare-stadion": "node steps/prepare-rondo-club-members.js",
    ```
    Change the script name from `prepare-stadion` to `prepare-rondo-club`:
    ```
    "prepare-rondo-club": "node steps/prepare-rondo-club-members.js",
    ```

    Also verify no other stadion references exist in either file.
  </action>
  <verify>
    Run: `grep -i 'stadion' CLAUDE.md package.json` — should return zero results.
  </verify>
  <done>
    CLAUDE.md references rondo_club_id instead of stadion_id. package.json script is named prepare-rondo-club instead of prepare-stadion.
  </done>
</task>

</tasks>

<verification>
1. `grep -ri 'stadion' docs/ CLAUDE.md package.json README.md` — zero results
2. All docs/ files render valid Markdown (no broken links from heading renames)
3. Table/column names in docs match actual schema (rondo_club_members, rondo_club_id, etc.)
</verification>

<success_criteria>
- Zero stadion references in docs/, CLAUDE.md, package.json, README.md
- All 200+ stadion occurrences in docs/ replaced with correct rondo_club equivalents
- SQL examples, code snippets, and prose all use rondo_club naming consistently
- Anchors and heading references updated where applicable
</success_criteria>

<output>
After completion, create `.planning/phases/43-documentation/43-01-SUMMARY.md`
</output>
